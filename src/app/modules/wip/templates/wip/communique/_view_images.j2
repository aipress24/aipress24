{# Bug 0128: same Alpine-driven carousel as the NEWS press-release page, #}
{# rendered below the form on the WIP communiqué "Voir" page so authors  #}
{# can confirm the images they uploaded for the CP.                       #}
<section class="mt-8 pt-6 border-t border-gray-200">
  <h2 class="text-lg font-medium text-gray-900 mb-4">Images</h2>
  <div class="my-4">
    {{ carousel(post, img_class="min-h-64") }}
  </div>
</section>
