{% extends "wip/layout/_base.j2" %}

{% block body_content %}
    <div class="px-4 sm:px-6 lg:px-8 max-w-3xl">
        <h1 class="text-2xl font-semibold text-gray-900">{{ title }}</h1>
        <p class="mt-2 text-sm text-gray-700">
            Notification libre — si vous partez d'un avis d'enquête, préférez
            le bouton « Notifier une publication » sur cet avis. Les
            destinataires sont recherchés parmi les membres actifs
            d'AiPRESS24.
        </p>

        <form method="POST" class="mt-6 space-y-6">
            <div>
                <label for="article_url"
                       class="block text-sm font-medium text-gray-700">
                    URL de l'article <span class="text-red-600">*</span>
                </label>
                <input id="article_url" name="article_url" type="url" required
                       placeholder="https://aipress24.com/wire/article/… ou site externe"
                       class="mt-1 block w-full rounded border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 text-sm px-3 py-2"/>
            </div>

            <div>
                <label for="article_title"
                       class="block text-sm font-medium text-gray-700">
                    Titre de l'article
                </label>
                <input id="article_title" name="article_title" type="text"
                       class="mt-1 block w-full rounded border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 text-sm px-3 py-2"/>
            </div>

            <div>
                <label for="message"
                       class="block text-sm font-medium text-gray-700">
                    Message personnel (optionnel)
                </label>
                <textarea id="message" name="message" rows="4"
                          maxlength="500"
                          class="mt-1 block w-full rounded border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 text-sm px-3 py-2"></textarea>
            </div>

            <div>
                <label for="recipient_ids"
                       class="block text-sm font-medium text-gray-700">
                    Destinataires <span class="text-red-600">*</span>
                </label>
                <select id="recipient_ids" name="recipient_ids"
                        multiple required size="12"
                        class="mt-1 block w-full rounded border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 text-sm">
                    {% for u in users %}
                        <option value="{{ u.id }}">
                            {{ u.last_name or '' }} {{ u.first_name or '' }}
                            — {{ u.email }}
                        </option>
                    {% endfor %}
                </select>
                <p class="mt-1 text-xs text-gray-500">
                    Ctrl/Cmd-clic pour sélectionner plusieurs destinataires.
                </p>
            </div>

            <div class="flex justify-between items-center border-t pt-4">
                <a href="{{ url_for('wip.notifications_publication_index') }}"
                   class="text-sm text-gray-700 hover:text-gray-900">
                    ← Retour à la liste
                </a>
                <button type="submit"
                        class="rounded bg-indigo-600 px-4 py-2 text-sm font-semibold text-white hover:bg-indigo-700">
                    Envoyer la notification
                </button>
            </div>
        </form>
    </div>
{% endblock %}
