{# Modal for content share #}
<div
    id="content-share-modal"
    class="fixed inset-0 z-50 overflow-y-auto"
    role="dialog"
    aria-modal="true"
    aria-labelledby="share-modal-title"
>
    <div class="flex items-end justify-center min-h-screen pt-4 px-4 pb-20 text-center sm:block sm:p-0">
        <div
            class="fixed inset-0 bg-gray-500 bg-opacity-75 transition-opacity"
            onclick="document.getElementById('content-share-modal')?.remove()"
            aria-hidden="true"
        ></div>

        <span class="hidden sm:inline-block sm:align-middle sm:h-screen"
              aria-hidden="true">&#8203;
        </span>

        <div class="inline-block align-bottom bg-white rounded-lg px-4 pt-5 pb-4 text-left overflow-hidden shadow-xl transform transition-all sm:my-8 sm:align-middle sm:max-w-lg sm:w-full sm:p-6">
            <div class="sm:flex sm:items-start">
                <div class="mx-auto flex-shrink-0 flex items-center justify-center h-12 w-12 sm:mx-0 sm:h-10 sm:w-10">
                    {{ icon("share", class="h-8 w-8 text-gray-700") }}
                </div>
                <div class="mt-3 text-center sm:mt-0 sm:ml-4 sm:text-left w-full">
                    <h3 class="text-lg leading-6 font-medium text-black"
                        id="share-modal-title">
                        Partager cette publication
                    </h3>
                    <div class="mt-1">
                        <p class="text-sm text-gray-700 truncate"
                           title="{{ post.title }}">
                            {{ post.title }}
                        </p>
                    </div>
                </div>
            </div>

            <div class="mt-4 space-y-4">
                <div>
                    <label for="share-url"
                           class="block text-sm font-medium text-gray-700 mb-1">
                        Lien de la publication
                    </label>
                    <input
                        id="share-url"
                        type="text"
                        readonly
                        value="{{ post_url }}"
                        class="block w-full px-3 py-2 rounded-md border border-gray-300 bg-gray-50 text-gray-700 text-sm focus:outline-none"
                    />
                </div>
            </div>

            <div class="mt-5 sm:mt-4 sm:flex sm:flex-row-reverse">
                <button
                    type="button"
                    class="w-full inline-flex justify-center rounded-md border border-gray-300 shadow-sm px-4 py-2 bg-white text-base font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 sm:w-auto sm:text-sm"
                    onclick="document.getElementById('content-share-modal')?.remove()"
                >
                    Fermer
                </button>
            </div>
        </div>
    </div>
</div>
