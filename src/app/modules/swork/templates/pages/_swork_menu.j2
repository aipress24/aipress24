<div class="mt-8 lg:col-span-2">
  <nav aria-label="Sidebar" class="sticky top-4 divide-y divide-gray-300">

    <div class="pb-8 space-y-1">
      {% for menu_item in menus.secondary %}
        {% if menu_item.active %}
          {% set class="bg-gray-200 text-gray-900 group flex items-center px-3 py-2 text-sm font-medium rounded-md" %}
        {% else %}
          {% set class="text-gray-900 group flex items-center px-3 py-2 text-sm font-medium rounded-md" %}
        {% endif %}

        <a
            href="{{ menu_item.url }}"
            class="{{ class }}"
            aria-current="page"
        >
          {{ icon(menu_item.icon, class="h-6 w-6 text-gray-400") }}
          <span class="truncate ml-2"> {{ menu_item.label }} </span>
        </a>
      {% endfor %}
    </div>
  </nav>
</div>

{#{% set menus_secondary = [] %}#}
{##}
{#<aside class="py-6 lg:col-span-2">#}
{#  <nav aria-label="Sidebar" class="sticky top-4 divide-y divide-gray-300 space-y-1">#}
{#    {% for item in menus_secondary %}#}
{#      {% if item.current %}#}
{#        {% set class="bg-teal-50 border-teal-500 text-teal-700 hover:bg-teal-50 hover:text-teal-700" %}#}
{#      {% else %}#}
{#        {% set class="border-transparent text-gray-900 hover:bg-gray-50 hover:text-gray-900" %}#}
{#      {% endif %}#}
{##}
{#      <a#}
{#          href="{{ item.href }}"#}
{#          class="group border-l-4 px-3 py-2 flex items-center text-sm font-medium {{ class }}"#}
{#          :aria-current="item.current ? 'page' : undefined"#}
{#      >#}
{#        {% if item.current %}#}
{#          {% set class="text-teal-500 group-hover:text-teal-500" %}#}
{#        {% else %}#}
{#          {% set class="text-gray-400 group-hover:text-gray-500" %}#}
{#        {% endif %}#}
{##}
{#        {{ icon(item.icon, type="outline", class="flex-shrink-0 -ml-1 mr-3 h-6 w-6 " + class) }}#}
{##}
{#        <span class="truncate">#}
{#          {{ item.label }}#}
{#        </span>#}
{#      </a>#}
{#    {% endfor %}#}
{#  </nav>#}
{#</aside>#}
